pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
    }
    plugins {
        id 'com.google.devtools.ksp' version '1.6.0-1.0.2'
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "PassingGrade"
include ':app'